apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-kahiro-charts.fullname" . }}-test-connection"
  labels:
    {{- include "test-kahiro-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-kahiro-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
