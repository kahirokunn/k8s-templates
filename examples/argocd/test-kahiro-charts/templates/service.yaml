apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-kahiro-charts.fullname" . }}
  labels:
    {{- include "test-kahiro-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-kahiro-charts.selectorLabels" . | nindent 4 }}
